@model Team15_FinalProject.Models.Checking

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Checking account details</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.CheckingName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.CheckingName)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.CheckingBalance)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.CheckingBalance)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Status)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Status)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Owner.FName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Owner.FName)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Owner.LName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Owner.LName)
        </dd>

    </dl>
</div>

<h4>Transactions</h4>

<table class="table">
    <tr>
        <th>
            Transaction Description
        </th>
        <th>
            Transaction Type
        </th>
        <th>
            Amount
        </th>
        <th>
            Date
        </th>
    </tr>

    @foreach (var item in ViewBag.transactions)
    {
        <tr>
            <td>
                @item.Description
            </td>
            <td>
                @item.TransactionType
            </td>
            <td>
                @item.Amount
            </td>
            <th>
                @item.Date
            </th>
        </tr>
    }

</table>

<h4>Disputes</h4>

<table class="table">
    <tr>
        <th>
            Dispute Status
        </th>
        <th>
            Comment
        </th>
    </tr>

    @foreach (var item in ViewBag.disputes)
    {
        <tr>
            <td>
                @item.Status
            </td>
            <td>
                @item.DisputeComment
            </td>
        </tr>
    }

</table>

<p>
    @Html.ActionLink("Edit", "CustomerEdit", new { id = Model.CheckingID }) |
    @Html.ActionLink("Back to List", "Index")
</p>
